{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "justMyCode": true,
            "request": "launch",
            "program": "${file}",
            "envFile": "${workspaceFolder}/.venv",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}